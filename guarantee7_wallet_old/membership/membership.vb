Public Class membership

    Public Shared Sub exe()

        'GR.authentication.id = "retelf"
        'GR.authentication.password = "#cyndi$36%"
        'GR.authentication.name_english = "jongbae park"
        'GR.authentication.name_home_language = "박종배"
        'GR.authentication.country = "south korea"
        'GR.authentication.phone_number = "010-6571-5154"
        'GR.authentication.identity_number = "630205-1047759"

        ''Dim authenticated = mobile_authentication.exe()
        'Dim authenticated = True

        'If authenticated Then

        '    Call generate_super_account.exe()

        'Else
        '    MessageBox.Show("본인 인증에 실패하였습니다.")
        'End If

    End Sub

End Class
